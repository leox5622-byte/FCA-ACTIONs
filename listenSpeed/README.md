# listenSpeed

Alternative high-speed MQTT listener. **CURRENTLY UNAVAILABLE** — fails to load due to missing `websocket-stream` dependency.

## Notes

- Requires `websocket-stream` npm package which is not installed
- Cannot be loaded — nkxfca logs: `Failed to load API module listenSpeed: Error: Cannot find module 'websocket-stream'`
- Use `listenMqtt` instead for standard MQTT event listening
## Roadmap

**Phase:** Maintenance
**Category:** Broken
**Status:** Broken
**Next Steps:** Missing websocket-stream dependency - install package